# Stage·Assign — state-of-the-app audit (2026-07-06)

_Run after shipping the full repair list + Report-a-bug (#2) + the SideStage review (#1).
Live build: **2026-07-06·g**. This is a health snapshot, not a to-do._

## Verdict: 🟢 healthy

Everything shipped this session is deployed, tested, and **runtime-clean** — a full
browser click-through across every major surface produced **zero console errors**.

## Automated gates
- `npm run check` → **JS syntax OK; CSS balanced** (1358 rules).
- `npm test` → **SUITE GREEN** — 64 test files, **0 real failures**, 1 known false-fail
  (`curve.js`, a jsdom stub artifact, documented in CLAUDE.md — not a bug).
- New test coverage added this session: `aurora.js`, `packnames.js`, `packconflict.js`,
  `packdialog.js`, `bugreport.js`, `quickwins.js`, plus additions to `setupmgr.js`,
  `shadowflow.js`, `setupcheckoff.js`, `checklist.js`, `mdpull.js`, `display.js`.

## Runtime QA (headless Chromium, real render)
Seeded a realistic roster (2 vocalists, 3 band incl. a deliberate Misc-1 collision, an
off-stage-IEM shadow) and exercised every surface:

| Surface | Result |
|---|---|
| All render fns (all, display, layout, setup-items, checklist, band, stage) | ✅ no throws |
| Theme + mood switching (molten/graphite, dark/light) | ✅ |
| Conflict engine (`findPackConflicts`) | ✅ 2 conflicts detected |
| Conflict resolution dialog | ✅ 2 fix rows render |
| Report-a-bug form | ✅ renders |
| **Console errors/warnings (whole run)** | ✅ **none** |

## What shipped this session (all live at ·g)
- **Bugs:** stage-layout edit full-screen (#7), checklist no-scroll-jump (#2).
- **Setup cluster:** solo-MD setup (#3), one merged checklist per person incl. linked
  instruments (#4).
- **Quick wins:** band IEM column label (#6), display auto-fullscreen + de-duped buttons
  (#5), Report-a-bug button (#1→superseded by the form below).
- **Aurora** became the default look, display view dialed in, mood preview swatches,
  wizard look step (#8).
- **IEM pack rework (#9):** dropped the "Pack" suffix + migration; conflict engine
  (off-stage-shadow-lowest priority); resolution dialog (PCO pull + add-person); live ⚠
  topbar badge.
- **Report-a-bug v2:** in-app form → downloads config + opens a prefilled GitHub issue.

## Notes / small follow-ups (not blockers)
1. **Brand tab is now inert.** With Aurora owning colors + fonts, the old Advanced
   Settings → Brand accent/font pickers no longer do anything. Consider hiding it or
   replacing with a note, so it doesn't confuse. (~15-min cleanup.)
2. **Stale backlog.** `docs/StageAssign_Backlog.md` still lists shipped work (print
   diagram, multi-venue, nav/naming) as open. Worth a prune so it reflects reality.
3. **Report-a-bug reading loop.** The `gh` CLI isn't available in this environment, so I
   can't `gh issue list` incoming reports directly. Until a proxy/auto-attach exists, I'll
   read a reported issue by URL (WebFetch) or you paste it. Also verify the repo has a
   `bug` label (GitHub default on most repos; the issue URL passes `labels=bug`).
4. **Single-file size:** `index.html` is ~12,900 lines. Fine and intentional (GOLDEN RULE
   1), but worth being mindful as features accrue — the pack + Aurora systems are now
   sizable. No action needed.
5. **Booth-only checks still pending your eyes** (jsdom/headless can't fully judge):
   Aurora display feel across moods on the real TV; the conflict dialog in a live PCO
   pull; and the end-to-end Report-a-bug → GitHub issue flow while logged in.

## Suggested next (your call)
- Tidy items 1–2 above (quick).
- Decide the SideStage-inspired direction (see `sidestage-comparison.md`): the cheap wins
  are a **live countdown / idle screen** on the display and maybe **headshots**; the
  strategic fork is **live TV sync** (needs a backend).
